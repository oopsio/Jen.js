{
  "name": "jen-js-blog-example",
  "version": "1.0.0",
  "description": "Production-grade blog example built with Jen.js",
  "type": "module",
  "scripts": {
    "dev": "node server.js dev",
    "start": "node server.js start",
    "build": "node build.js",
    "typecheck": "tsc -p ../../tsconfig.json --noEmit",
    "clean": "rmdir /s /q dist .esbuild 2>nul || exit 0"
  },
  "dependencies": {
    "esbuild": "^0.27.3",
    "preact": "^10.28.3",
    "preact-render-to-string": "^6.5.13",
    "sirv": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
