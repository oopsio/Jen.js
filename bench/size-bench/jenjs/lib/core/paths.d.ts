export declare function resolveSitePath(
  config: {
    siteDir: string;
  },
  ...p: string[]
): string;
export declare function resolveDistPath(
  config: {
    distDir: string;
  },
  ...p: string[]
): string;
