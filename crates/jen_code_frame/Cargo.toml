[package]
name = "jen_code_frame"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
syntect = { version = "5.0", default-features = false, features = ["default-fancy"] }