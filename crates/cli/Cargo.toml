[package]
name = "jen_launcher"
version = "0.1.0"
edition = "2021"
description = "Tiny Node.js launcher"
license = "MIT"

[profile.release]
# Strips debug info, optimizes for size
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true